# Historical provenance

These manifests record earlier source imports and verification milestones.
The import-era manifests preserve their original module paths from before
the single-library reorganization. The
[pre-BBR-closure snapshot](pre-bbr-influence-20260910-verification.json)
preserves the 2026-09-09 single-library verification before the internal
influence identity and its stronger public theorem signatures were added.
The [pre-near-Vigoda-CI snapshot](pre-near-vigoda-ci-20260924-verification.json)
preserves the 2026-09-10 record before the packaged near-Vigoda coupling
theorems were added. The [pre-cited-proofs snapshot](pre-cited-proofs-20260924-verification.json)
preserves the 2026-09-24 record before the cited CLMM, BBR and critical-line
results were proved and removed from the theorem hypotheses. The
[pre-coverage snapshot](pre-coverage-20260924-verification.json) preserves
the 2026-09-25 record before the remaining numbered statements of both
papers were formalized and the coverage table was added.

For the 2026-09-09 path moves, use [the module map](../module-moves.tsv). For the current
build, theorem coverage and source hashes, use [the verification record](../verification.json).
